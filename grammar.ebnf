program ::= { statement } exit
statement ::= decl | assign
decl ::= "i32" [ "mut" ] ident "{" expr "}"
assign ::= ident ":=" expr
exit ::= "exit" factor
expr ::= term { ("+" | "-") term }
term ::= factor { "*" factor }
factor ::= number | ident
